name: Deploy to Azure Web App (Docker)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Azure Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ sentimentregistrysaiku }}
        username: ${{ sentimentregistrysaiku }}
        password: ${{ JdMkSp9JpgZK7IZFhOJ+Uz2M8VzKpfOmt72JY7mKlB+ACRDdMuVS }}

    - name: Build Docker image
      run: |
        docker build -t ${{ sentimentregistrysaiku.azurecr.io }}/sentiment_app:${{ github.sha }} .

    - name: Push Docker image to ACR
      run: |
        docker push ${{ sentimentregistrysaiku.azurecr.io }}/sentiment_app:${{ github.sha }}

    - name: Deploy to Azure App Service
      uses: azure/webapps-deploy@v3
      with:
        app-name: ${{ sentimentdockerapp }}
        images: ${{ sentimentregistrysaiku.azurecr.io }}/sentiment_app:${{ github.sha }}
